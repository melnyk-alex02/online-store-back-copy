spring.datasource.url=jdbc:postgresql://localhost:5432/storetest
spring.datasource.password=${DB_PASSWORD}
spring.datasource.username=${DB_USER}

spring.liquibase.change-log=classpath:db.changeLog-test/changeLog-master.xml
spring.liquibase.drop-first=true

spring.security.oauth2.resource.jwk.key-set-uri=${KEYCLOAK_REALM_CERT_URL:http://keycloak:8080/realms/happytails/protocol/openid-connect/certs}
